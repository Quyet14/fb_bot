# TODO

- [x] Gắn version `?v=` cho `ui-design/html/api.js` và `ui-design/html/app.js` trong các trang: `ui-design/pages/groups.html`, `topics.html`, `schedules.html`, `logs.html`, để tránh browser cache giữ code cũ.

- [ ] (Sau khi sửa UI) Hard refresh / xóa cache trong trình duyệt để kiểm tra thay đổi.
- [x] Restart backend FastAPI sau khi sửa code Python (đặc biệt các file scheduler/crud/routers) để đảm bảo server chạy bản mới.

- [ ] Kiểm tra `settings.USE_MONGODB` để xác nhận đang chạy đúng backend/data layer bạn đang sửa.

